<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Custom theme. -->
        <item name="colorPrimary">@color/customPrimaryColor</item>
        <item name="colorPrimaryDark">@color/customPrimaryDarkColor</item>
        <item name="colorAccent">@color/customAccentColor</item>
        <item name="android:textColorPrimary">@color/customTextPrimaryColor</item>
        <item name="android:windowBackground">@color/customWindowBackground</item>
        <item name="actionMenuTextColor">@color/abc_primary_text_material_dark</item>
        <item name="android:textColorSecondary">@color/abc_secondary_text_material_dark</item>
    </style>

</resources>
